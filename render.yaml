services:
  - type: web
    name: zagel
    runtime: docker
    plan: starter
    healthCheckPath: /api/health
    disk:
      name: zagel-data
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_API_KEY
        generateValue: true

